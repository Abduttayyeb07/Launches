# Zig RPC WebSocket endpoint — DO NOT hardcode in source
RPC_WS=wss://rpc.zigscan.net/websocket

# MDF contract address on Zig chain
MDF_CONTRACT=zig1############################################

# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Optional: base URL for token metadata enrichment API
# If set, will call ${MDF_TOKEN_API_BASE}/{denom} when metadata is incomplete
# Leave empty to disable enrichment
MDF_TOKEN_API_BASE=

# Log level: debug | info | warn | error (default: info)
LOG_LEVEL=info
